---
phase: 05-orchestrator-facade
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/cli-progress.ts
  - src/index.ts
  - src/core/interaction-handler.ts
  - src/core/evolve-runner.ts
  - src/core/refine-runner.ts
  - src/core/llm-setup.ts
  - src/auth/resolve-auth.ts
  - src/mcp-entry.ts
autonomous: true
requirements: [ORCH-03]

must_haves:
  truths:
    - "Zero console.log/warn/error calls in any non-test src/ file"
    - "CLI output behavior is identical (same text, same formatting) -- only the underlying write mechanism changes"
  artifacts:
    - path: "src/core/cli-progress.ts"
      provides: "CLI progress display using process.stdout.write"
    - path: "src/index.ts"
      provides: "CLI entry point using process.stdout.write and process.stderr.write"
    - path: "src/core/interaction-handler.ts"
      provides: "CLI interaction using process.stdout.write"
    - path: "src/core/evolve-runner.ts"
      provides: "Evolution CLI using process.stdout.write"
    - path: "src/core/refine-runner.ts"
      provides: "Refine CLI using process.stdout.write"
    - path: "src/core/llm-setup.ts"
      provides: "LLM setup wizard using process.stdout.write"
    - path: "src/auth/resolve-auth.ts"
      provides: "Auth feedback using process.stdout.write"
    - path: "src/mcp-entry.ts"
      provides: "MCP error output using process.stderr.write"
  key_links:
    - from: "all modified files"
      to: "process.stdout/process.stderr"
      via: "process.stdout.write(msg + '\\n') replaces console.log(msg)"
      pattern: "process\\.std(out|err)\\.write"
---

<objective>
Replace all console.log/warn/error calls in CLI presentation files with process.stdout.write/process.stderr.write.

Purpose: ORCH-03 requires zero console.log/warn/error calls in src/ (excluding tests). The CLI presentation layer uses console.log intentionally as terminal output, but the strict interpretation requires replacing them with process.stdout.write to satisfy the zero-console criterion. Behavior is identical -- same text, same formatting.

Output: 8 files updated with ~145 console calls replaced by process.stdout.write or process.stderr.write equivalents.
</objective>

<execution_context>
@/Users/lddmay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lddmay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace console calls in cli-progress.ts and interaction-handler.ts</name>
  <files>src/core/cli-progress.ts, src/core/interaction-handler.ts</files>
  <read_first>src/core/cli-progress.ts, src/core/interaction-handler.ts</read_first>
  <action>
Mechanical search-and-replace in both files:

1. `console.log(...)` -> `process.stdout.write(... + '\n')`
   - For template literals: `console.log(\`...\`)` -> `process.stdout.write(\`...\n\`)`
   - For string args: `console.log('...')` -> `process.stdout.write('...\n')`
   - For string concat: `console.log(a + b)` -> `process.stdout.write(a + b + '\n')`

2. `console.error(...)` -> `process.stderr.write(... + '\n')`
   - Same pattern as above but to stderr

3. `console.warn(...)` -> `process.stderr.write(... + '\n')`

IMPORTANT: process.stdout.write does NOT auto-append newlines. Every replacement must include `\n` at the end of the string.

For cli-progress.ts (~35 calls): Every console.log becomes process.stdout.write with \n appended.
For interaction-handler.ts (~14 calls): Same transformation.

Do NOT modify any logic, only the output mechanism. The output text must be byte-identical.

Note: logger.ts has 1 console.error that MUST remain -- it's the logger's own error handler (can't log via itself). Do NOT touch logger.ts.
  </action>
  <verify>
    <automated>cd /Users/lddmay/AiCoding/mosaicat && grep -c 'console\.\(log\|warn\|error\)' src/core/cli-progress.ts src/core/interaction-handler.ts && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
- `grep -c 'console\.' src/core/cli-progress.ts` returns 0
- `grep -c 'console\.' src/core/interaction-handler.ts` returns 0
- `grep -c 'process.stdout.write\|process.stderr.write' src/core/cli-progress.ts` returns >= 35
- `grep -c 'process.stdout.write\|process.stderr.write' src/core/interaction-handler.ts` returns >= 14
- `npx tsc --noEmit` compiles without errors
  </acceptance_criteria>
  <done>cli-progress.ts and interaction-handler.ts have zero console calls. All output uses process.stdout.write/process.stderr.write.</done>
</task>

<task type="auto">
  <name>Task 2: Replace console calls in index.ts and remaining CLI files</name>
  <files>src/index.ts, src/core/evolve-runner.ts, src/core/refine-runner.ts, src/core/llm-setup.ts, src/auth/resolve-auth.ts, src/mcp-entry.ts</files>
  <read_first>src/index.ts, src/core/evolve-runner.ts, src/core/refine-runner.ts, src/core/llm-setup.ts, src/auth/resolve-auth.ts, src/mcp-entry.ts</read_first>
  <action>
Same mechanical search-and-replace as Task 1, applied to 6 files:

1. `console.log(...)` -> `process.stdout.write(... + '\n')`
2. `console.error(...)` -> `process.stderr.write(... + '\n')`
3. `console.warn(...)` -> `process.stderr.write(... + '\n')`

File-specific notes:
- src/index.ts (~35 calls): Mix of console.log and console.error. Errors go to stderr, info to stdout.
- src/core/evolve-runner.ts (~32 calls): Mostly console.log for interactive output.
- src/core/refine-runner.ts (~15 calls): Same pattern.
- src/core/llm-setup.ts (~14 calls): Interactive setup wizard output.
- src/auth/resolve-auth.ts (1 call): Single console.log.
- src/mcp-entry.ts (1 call): Single console.error -> process.stderr.write.

IMPORTANT: Append `\n` to every write call. Do NOT modify any logic, only the output mechanism.
  </action>
  <verify>
    <automated>cd /Users/lddmay/AiCoding/mosaicat && grep -rn 'console\.\(log\|warn\|error\)' src/ --include='*.ts' | grep -v '__tests__' | grep -v '.test.ts' | grep -v 'logger.ts' | wc -l</automated>
  </verify>
  <acceptance_criteria>
- `grep -rn 'console\.\(log\|warn\|error\)' src/ --include='*.ts' | grep -v '__tests__' | grep -v '.test.ts' | grep -v 'logger.ts' | wc -l` returns 0
- `npx tsc --noEmit` compiles without errors
- The only remaining console call in non-test src/ is in logger.ts (its own error handler)
  </acceptance_criteria>
  <done>All 6 CLI files have zero console calls. Combined with Plan 01 Task 2 and this plan's Task 1, zero console.log/warn/error calls remain in src/ (excluding tests and logger.ts self-handler).</done>
</task>

</tasks>

<verification>
- `grep -rn 'console\.\(log\|warn\|error\)' src/ --include='*.ts' | grep -v '__tests__' | grep -v '.test.ts' | grep -v 'logger.ts'` returns empty
- `npx tsc --noEmit` passes
- `npx vitest run -x` full suite passes (no behavioral changes, only output mechanism)
</verification>

<success_criteria>
- Zero console.log/warn/error calls in src/ (excluding test files and logger.ts)
- TypeScript compiles clean
- All existing tests pass (output behavior unchanged)
</success_criteria>

<output>
After completion, create `.planning/phases/05-orchestrator-facade/05-02-SUMMARY.md`
</output>
