---
phase: 05-orchestrator-facade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/event-bus.ts
  - src/core/retrying-provider.ts
  - src/core/snapshot.ts
  - src/core/git-publisher.ts
  - src/evolution/__tests__/proposal-handler.test.ts
autonomous: true
requirements: [ORCH-02, ORCH-03]

must_haves:
  truths:
    - "EventBus singleton export no longer exists in event-bus.ts"
    - "No production code imports the deprecated eventBus singleton"
    - "Infrastructure modules (retrying-provider, snapshot, git-publisher) use process.stderr.write instead of console.warn"
  artifacts:
    - path: "src/core/event-bus.ts"
      provides: "EventBus class export only, no singleton"
      contains: "export { EventBus }"
    - path: "src/core/retrying-provider.ts"
      provides: "Retry logging via process.stderr.write"
    - path: "src/core/snapshot.ts"
      provides: "Error logging via process.stderr.write"
    - path: "src/core/git-publisher.ts"
      provides: "Warning via process.stderr.write"
  key_links:
    - from: "src/evolution/__tests__/proposal-handler.test.ts"
      to: "src/core/event-bus.ts"
      via: "import { EventBus } instead of eventBus singleton"
      pattern: "new EventBus"
---

<objective>
Remove the EventBus singleton export and replace 3 console.warn calls in infrastructure modules with process.stderr.write.

Purpose: ORCH-02 requires EventBus to be instance-scoped (already done in Phase 2 via RunContext, just need to remove the deprecated singleton export). ORCH-03 requires eliminating console calls from infrastructure modules that bypass Logger.

Output: event-bus.ts exports only the EventBus class; retrying-provider.ts, snapshot.ts, git-publisher.ts use process.stderr.write.
</objective>

<execution_context>
@/Users/lddmay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lddmay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- EventBus class and deprecated singleton from event-bus.ts -->
From src/core/event-bus.ts:
```typescript
class EventBus {
  emit<E extends EventName>(event: E, ...args: Parameters<PipelineEvents[E]>): void;
  on<E extends EventName>(event: E, handler: PipelineEvents[E]): void;
  off<E extends EventName>(event: E, handler: PipelineEvents[E]): void;
  removeAllListeners(): void;
}

/** @deprecated Use RunContext.eventBus instead. */
export const eventBus = new EventBus();
export { EventBus };
```

From src/evolution/__tests__/proposal-handler.test.ts (line 12):
```typescript
import { eventBus } from '../../core/event-bus.js';
```

From src/core/retrying-provider.ts (line 128):
```typescript
console.warn(`[retry] attempt ${attempt}, waiting ${Math.round(delay)}ms: ${error.message}`);
```

From src/core/snapshot.ts (line 59):
```typescript
console.warn(`[snapshot] Failed to copy ${srcPath}: ${err instanceof Error ? err.message : String(err)}`);
```

From src/core/git-publisher.ts (line 57):
```typescript
console.warn(`[git-publisher] Skipping missing file: ${filePath}`);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove EventBus singleton export + fix test import</name>
  <files>src/core/event-bus.ts, src/evolution/__tests__/proposal-handler.test.ts</files>
  <read_first>src/core/event-bus.ts, src/evolution/__tests__/proposal-handler.test.ts</read_first>
  <action>
In src/core/event-bus.ts:
- Delete lines 60-65 (the @deprecated JSDoc comment and `export const eventBus = new EventBus();`)
- Keep `export { EventBus };` on what is currently line 67
- The file should export ONLY the `EventBus` class and `PipelineEvents` interface

In src/evolution/__tests__/proposal-handler.test.ts:
- Change `import { eventBus } from '../../core/event-bus.js';` to `import { EventBus } from '../../core/event-bus.js';`
- Add `const eventBus = new EventBus();` immediately after the import block
- The test should create its own instance instead of using the removed singleton

Per ORCH-02: EventBus must be instance-scoped, passed via RunContext. The singleton was the last legacy export.
  </action>
  <verify>
    <automated>cd /Users/lddmay/AiCoding/mosaicat && npx tsc --noEmit 2>&1 | head -20 && grep -c 'export const eventBus' src/core/event-bus.ts; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
- `grep 'export const eventBus' src/core/event-bus.ts` returns 0 matches
- `grep -c 'new EventBus()' src/evolution/__tests__/proposal-handler.test.ts` returns 1
- `npx tsc --noEmit` compiles without errors
- `npx vitest run src/evolution/__tests__/proposal-handler.test.ts -x` passes
  </acceptance_criteria>
  <done>EventBus singleton export removed. Only the EventBus class is exported. Test file creates its own instance.</done>
</task>

<task type="auto">
  <name>Task 2: Replace console.warn with process.stderr.write in infrastructure modules</name>
  <files>src/core/retrying-provider.ts, src/core/snapshot.ts, src/core/git-publisher.ts</files>
  <read_first>src/core/retrying-provider.ts, src/core/snapshot.ts, src/core/git-publisher.ts</read_first>
  <action>
In src/core/retrying-provider.ts (line 128):
- Replace: `console.warn(\`[retry] attempt ${attempt}, waiting ${Math.round(delay)}ms: ${error.message}\`);`
- With: `process.stderr.write(\`[retry] attempt ${attempt}, waiting ${Math.round(delay)}ms: ${error.message}\n\`);`
- Note the explicit `\n` at the end -- process.stderr.write does not auto-append newlines like console.warn does.

In src/core/snapshot.ts (line 59):
- Replace: `console.warn(\`[snapshot] Failed to copy ${srcPath}: ${err instanceof Error ? err.message : String(err)}\`);`
- With: `process.stderr.write(\`[snapshot] Failed to copy ${srcPath}: ${err instanceof Error ? err.message : String(err)}\n\`);`

In src/core/git-publisher.ts (line 57):
- Replace: `console.warn(\`[git-publisher] Skipping missing file: ${filePath}\`);`
- With: `process.stderr.write(\`[git-publisher] Skipping missing file: ${filePath}\n\`);`

Per ORCH-03: These 3 infrastructure modules should not use console.log/warn/error. They are pipeline internals, not CLI presentation.
  </action>
  <verify>
    <automated>cd /Users/lddmay/AiCoding/mosaicat && grep -rn 'console\.\(log\|warn\|error\)' src/core/retrying-provider.ts src/core/snapshot.ts src/core/git-publisher.ts; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c 'console\.' src/core/retrying-provider.ts` returns 0
- `grep -c 'console\.' src/core/snapshot.ts` returns 0
- `grep -c 'console\.' src/core/git-publisher.ts` returns 0
- `grep -c 'process.stderr.write' src/core/retrying-provider.ts` returns 1
- `grep -c 'process.stderr.write' src/core/snapshot.ts` returns 1
- `grep -c 'process.stderr.write' src/core/git-publisher.ts` returns 1
- `npx tsc --noEmit` compiles without errors
  </acceptance_criteria>
  <done>All 3 infrastructure modules use process.stderr.write instead of console.warn. Zero console calls remain in these files.</done>
</task>

</tasks>

<verification>
- `grep -rn 'export const eventBus' src/core/event-bus.ts` returns empty (singleton removed)
- `grep -rn 'console\.\(log\|warn\|error\)' src/core/retrying-provider.ts src/core/snapshot.ts src/core/git-publisher.ts` returns empty
- `npx tsc --noEmit` passes
- `npx vitest run src/evolution/__tests__/proposal-handler.test.ts -x` passes
</verification>

<success_criteria>
- EventBus singleton export deleted from event-bus.ts
- Zero console.log/warn/error calls in retrying-provider.ts, snapshot.ts, git-publisher.ts
- All tests pass, TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/05-orchestrator-facade/05-01-SUMMARY.md`
</output>
