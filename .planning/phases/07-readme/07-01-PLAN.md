---
phase: 07-readme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08

must_haves:
  truths:
    - "README.md contains no stale 'infinite retry' claims -- all LLM retry descriptions mention bounded retry (max 20) + circuit breaker"
    - "README.md leads with demo output before comparison table, per reorganized structure"
    - "README.md architecture diagram includes PipelineLoop, StageExecutor, FixLoopRunner, ShutdownCoordinator"
    - "README.md contains no TODO HTML comments"
    - "README.md uses dev-mode CLI commands (npx tsx src/index.ts) not npx mosaicat"
    - "README.md tone is technical documentation, not marketing copy"
  artifacts:
    - path: "README.md"
      provides: "Chinese README with v2-accurate content, reorganized structure, technical tone"
      contains: "熔断"
  key_links:
    - from: "README.md"
      to: "src/core/retrying-provider.ts"
      via: "accurate description of bounded retry + circuit breaker behavior"
      pattern: "熔断|circuit.breaker"
    - from: "README.md"
      to: "src/core/fix-loop-runner.ts"
      via: "accurate description of 5-round progressive fix strategy"
      pattern: "direct-fix.*replan.*full-history"
---

<objective>
Rewrite README.md (Chinese) to reflect v2 architecture accurately, reorganize section order for AI-savvy developers, shift tone from marketing to technical, fix all stale claims, remove TODO placeholders, and add terminal output demo block.

Purpose: The current README contains multiple technical inaccuracies from the v2 core engine rewrite (phases 2-6) and has a marketing-heavy tone inappropriate for the target audience.

Output: Updated README.md with all D-01 through D-08 decisions implemented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-readme/07-CONTEXT.md
@.planning/phases/07-readme/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md with v2-accurate content, reorganized structure, and technical tone</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state, 507 lines)
    - .planning/phases/07-readme/07-CONTEXT.md (locked decisions D-01 through D-08)
    - .planning/phases/07-readme/07-RESEARCH.md (v2 technical facts checklist, recommended section order, inaccurate claims list)
    - src/core/retrying-provider.ts (verify: max 20 retries, circuit breaker config -- 5 failures, 30s recovery)
    - src/core/fix-loop-runner.ts (verify: 5-round progressive strategy -- rounds 1-2 direct-fix, round 3 replan, rounds 4-5 full-history)
    - src/core/shutdown-coordinator.ts (verify: SIGINT/SIGTERM handling, AbortController)
    - src/core/orchestrator.ts (verify: thin facade, line count)
    - src/core/pipeline-loop.ts (verify: while-loop iteration)
    - src/core/stage-executor.ts (verify: single-stage execution, StageOutcome)
    - src/core/artifact-store.ts (verify: per-run instance scoping)
    - src/core/run-context.ts (verify: dependency bundle)
    - config/pipeline.yaml (profile definitions)
  </read_first>
  <action>
Rewrite README.md (Chinese) applying ALL decisions D-01 through D-08. The new file should follow this section order (per D-06, discretion on exact ordering for AI-savvy developers):

**Section 1: Header** (per D-07: remove `<!-- TODO: Replace with custom banner image -->` comment entirely, keep the shield.io badge and subtitle)

**Section 2: One-liner + nav links** (update nav links to match new section anchors)

**Section 3: Badges** (keep as-is)

**Section 4: Demo / Example output** (per D-08: replace `<!-- TODO: demo GIF -->` with a formatted code block showing realistic CLI progress output of a pipeline run, e.g.:
```
$ npx tsx src/index.ts run "做一个个人记账 App"

  Mosaicat v2 — Spec Coding Pipeline

  [1/13] IntentConsultant    ✓  intent-brief.json        12s
  [2/13] Researcher          ✓  research.md              45s
  [3/13] ProductOwner        ⏸  prd.md                   awaiting approval
         → approved
  [3/13] ProductOwner        ✓  prd.md                   38s
  [4/13] UXDesigner          ✓  ux-flows.md              52s
  ...
  [13/13] Validator          ✓  validation-report.md     8s

  ✓ Pipeline complete — 13 stages, 4 approvals, 18m 42s
  → .mosaic/artifacts/run-1774366900/
```
This is a representative example, not a real capture.)

**Section 5: Why Mosaicat / Core value proposition** (per D-05: rewrite to technical tone. Drop marketing flourishes like "paradigm shift". State what it does factually. Per D-01: fix ALL stale claims:
- Change "韧性优先 — LLM 调用无限重试" to "韧性优先 — LLM 调用有界重试（最多 20 次）+ 熔断器（5 次连续失败后断路，30s 恢复）+ Stage Resume 崩溃恢复"
- Keep the 5 bullet points but reword to be technical, not promotional)

**Section 6: Key Features list** (per D-01: fix all stale claims:
- Change "LLM 无限重试 — 指数退避自动重试暂时性错误...只有不可恢复的错误才终止" to "LLM 有界重试 + 熔断器 — 指数退避重试瞬时错误（429, 503, 网络），最多 20 次；连续 5 次失败触发熔断器断路（30s HALF_OPEN 恢复）"
- Update "5 轮渐进式修复循环" description to include strategy names: "rounds 1-2 direct-fix → round 3 replan-failed-modules → rounds 4-5 full-history-fix"
- Add mention of graceful shutdown: "优雅关闭 — SIGINT/SIGTERM 触发 ShutdownCoordinator，完成当前 stage 写入后退出，不留部分 artifact"
Per D-03: keep `npx tsx src/index.ts` commands throughout)

**Section 7: Comparison table** (per D-06: moved earlier, right after features. Per D-01: fix row "LLM 无限重试 | ✅ 指数退避" to "LLM 有界重试 + 熔断 | ✅ 最多 20 次 + 熔断器")

**Section 8: Quick Start** (per D-06: consolidate prerequisites into quick start as a subsection. Per D-03: keep `npx tsx src/index.ts` commands. Merge "前置要求" section INTO quick start as first subsection. Keep all usage modes (CLI, GitHub, MCP, refine, evolve) together here.)

**Section 9: How It Works** (keep pipeline mermaid diagram but update the fix loop label from `"fix loop ×5"` to `"fix loop ×5\n(direct→replan→full-history)"` or similar to reflect progressive strategy. Keep agent table as-is -- it's accurate.)

**Section 10: Pipeline Profiles** (keep as-is, content is accurate)

**Section 11: Architecture** (per D-01: update mermaid diagram in Engine subgraph:
- Rename "Orchestrator 编排器" to "Orchestrator\n(thin facade)"
- Add "PipelineLoop" node
- Add "StageExecutor" node
- Add "FixLoopRunner" node
- Add "ShutdownCoordinator" node
- Add "RunContext" node
- Update connections: Orch --> PipelineLoop, PipelineLoop --> StageExecutor, StageExecutor --> FixLoopRunner, etc.
- Remove "Resume" as standalone node (replaced by PipelineLoop's resume capability)
- Update RetryingProvider description to show bounded retry + circuit breaker
Add brief text descriptions of v2 modules after the diagram.)

**Section 12: Design Principles** (per D-05: trim marketing language. Per D-01: fix the "韧性优先" subsection:
- Change "RetryingProvider 装饰所有 LLM Provider，指数退避无限重试瞬时错误" to "RetryingProvider 装饰所有 LLM Provider，指数退避有界重试（最多 20 次）+ 熔断器保护（5 次连续失败后断路，30s HALF_OPEN 恢复）"
- Add ShutdownCoordinator mention: "ShutdownCoordinator 处理 SIGINT/SIGTERM，通过 AbortSignal 通知 PipelineLoop 在当前 stage 完成后优雅退出")

**Section 13: Outputs** (keep as-is, content is accurate)

**Section 14: Roadmap** (per D-01: fix M7 row -- change "LLM 无限重试" to "LLM 有界重试 + 熔断器". Add "v2 核心引擎重写" as a completed entry or note.)

**Section 15: Contributing + License** (keep as-is, but remove the `<!-- TODO: contrib.rocks -->` comment -- just leave the text)

**Remove all remaining HTML TODO comments.** Search for `TODO` and ensure zero remain.

**Remove the commented-out Star History section at the bottom** (lines 501-506) -- it contains TODO.

**Usage Modes table:** Keep but move into Quick Start section as a reference table rather than standalone section.
  </action>
  <verify>
    <automated>cd /Users/lddmay/AiCoding/mosaicat && echo "=== Stale claims ===" && grep -c "无限重试\|infinite.*retry\|Infinite.*retry" README.md && echo "=== TODO comments ===" && grep -c "TODO" README.md && echo "=== v2 modules ===" && grep -c "PipelineLoop\|StageExecutor\|FixLoopRunner\|ShutdownCoordinator" README.md && echo "=== Circuit breaker ===" && grep -c "熔断" README.md && echo "=== Dev-mode CLI ===" && grep -c "npx tsx src/index.ts" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "无限重试" README.md` returns 0 (zero stale "infinite retry" claims)
    - `grep -c "TODO" README.md` returns 0 (zero TODO comments remaining)
    - `grep -c "PipelineLoop" README.md` returns at least 1
    - `grep -c "StageExecutor" README.md` returns at least 1
    - `grep -c "FixLoopRunner" README.md` returns at least 1
    - `grep -c "ShutdownCoordinator" README.md` returns at least 1
    - `grep -c "熔断" README.md` returns at least 2 (circuit breaker mentioned in features + architecture)
    - `grep -c "npx tsx src/index.ts" README.md` returns at least 5 (dev-mode CLI preserved per D-03)
    - `grep -c "thin facade\|薄外层\|thin 外层" README.md` returns at least 1 (orchestrator described as facade)
    - `grep -c "direct-fix\|replan\|full-history" README.md` returns at least 1 (progressive fix strategy described)
    - README.md first `## ` section after header/badges is NOT "前置要求" (structure reorganized per D-06)
    - File does not contain `<!-- TODO` anywhere
  </acceptance_criteria>
  <done>README.md is fully rewritten with all 8 decisions (D-01 through D-08) implemented: v2-accurate technical claims, reorganized section order (demo first, comparison earlier, consolidated quick start), technical tone (no marketing flourishes), all TODO placeholders resolved, terminal demo code block present, architecture diagram updated with v2 modules.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.en.md to match updated README.md structure and content</name>
  <files>README.en.md</files>
  <read_first>
    - README.md (just-updated Chinese version -- the source of truth for structure and content)
    - README.en.md (current English version, 503 lines)
    - .planning/phases/07-readme/07-CONTEXT.md (D-02: keep both files consistent)
  </read_first>
  <action>
Translate the updated README.md into README.en.md, ensuring exact structural parity (per D-02).

Specific translation requirements:
- Section order MUST match README.md exactly (same number of `## ` headings, same order)
- Mermaid diagrams: use English labels (e.g., "Orchestrator\n(thin facade)" not "Orchestrator\n(薄外层)")
- Agent names stay in English (they are code identifiers): IntentConsultant, Researcher, ProductOwner, etc.
- Technical terms keep English originals: PipelineLoop, StageExecutor, FixLoopRunner, ShutdownCoordinator, RunContext, ArtifactStore, RetryingProvider, circuit breaker, HALF_OPEN
- CLI commands identical in both files: `npx tsx src/index.ts run`, `npx tsx src/index.ts resume`, etc.
- Terminal demo code block: translate the Chinese instruction example to English (e.g., "Build a personal finance tracker")
- Comparison table: same structure, English labels
- All v2 technical facts must match Chinese version exactly (max 20 retries, 5-failure circuit breaker, 30s recovery, progressive fix strategy names)
- Remove all TODO HTML comments (same as Chinese version)
- Remove commented-out Star History section

Verify structural parity: both files should have the same number of `## ` level-2 headings.
  </action>
  <verify>
    <automated>cd /Users/lddmay/AiCoding/mosaicat && echo "=== Stale claims ===" && grep -c "infinite.*retry\|Infinite.*retry" README.en.md && echo "=== TODO comments ===" && grep -c "TODO" README.en.md && echo "=== v2 modules ===" && grep -c "PipelineLoop\|StageExecutor\|FixLoopRunner\|ShutdownCoordinator" README.en.md && echo "=== Circuit breaker ===" && grep -c "circuit.breaker" README.en.md && echo "=== Section parity ===" && echo "CN sections:" && grep -c "^## " README.md && echo "EN sections:" && grep -c "^## " README.en.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "infinite.*retry\|Infinite.*retry" README.en.md` returns 0 (zero stale claims)
    - `grep -c "TODO" README.en.md` returns 0 (zero TODO comments)
    - `grep -c "PipelineLoop" README.en.md` returns at least 1
    - `grep -c "StageExecutor" README.en.md` returns at least 1
    - `grep -c "FixLoopRunner" README.en.md` returns at least 1
    - `grep -c "ShutdownCoordinator" README.en.md` returns at least 1
    - `grep -c "circuit.breaker" README.en.md` returns at least 2
    - `grep -c "npx tsx src/index.ts" README.en.md` returns at least 5
    - `grep -c "^## " README.md` equals `grep -c "^## " README.en.md` (section count parity)
    - `grep -c "direct-fix\|replan\|full-history" README.en.md` returns at least 1
    - File does not contain `<!-- TODO` anywhere
  </acceptance_criteria>
  <done>README.en.md matches README.md in structure (same section count, same order), all v2 technical claims are accurate, all TODO placeholders removed, terminal demo present in English, architecture diagram uses English labels with v2 modules.</done>
</task>

</tasks>

<verification>
Run the full grep validation suite to confirm both files are accurate and in sync:

```bash
# Stale claims (must be 0 for both)
grep -c "无限重试" README.md
grep -c "infinite.*retry\|Infinite.*retry" README.en.md

# TODO comments (must be 0 for both)
grep -c "TODO" README.md
grep -c "TODO" README.en.md

# v2 modules present (must be >= 1 for both)
grep -c "PipelineLoop\|StageExecutor\|FixLoopRunner\|ShutdownCoordinator" README.md
grep -c "PipelineLoop\|StageExecutor\|FixLoopRunner\|ShutdownCoordinator" README.en.md

# Section count parity
grep -c "^## " README.md
grep -c "^## " README.en.md

# Circuit breaker mentioned
grep -c "熔断" README.md
grep -c "circuit.breaker" README.en.md
```
</verification>

<success_criteria>
1. Both README files contain zero stale "infinite retry" claims
2. Both README files contain zero TODO HTML comments
3. Both README files mention PipelineLoop, StageExecutor, FixLoopRunner, ShutdownCoordinator
4. Both README files have identical section count (structural parity)
5. Architecture mermaid diagrams include v2 engine modules
6. Terminal demo code block present in both files
7. All CLI commands use `npx tsx src/index.ts` (dev mode per D-03)
8. RetryingProvider described with bounded retry (max 20) + circuit breaker (5 failures, 30s)
</success_criteria>

<output>
After completion, create `.planning/phases/07-readme/07-01-SUMMARY.md`
</output>
