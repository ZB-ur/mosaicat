---
phase: 01-test-infrastructure-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/__tests__/e2e-canary.test.ts
  - vitest.config.ts
autonomous: true
requirements:
  - TEST-03
must_haves:
  truths:
    - "A canary test runs a full 13-stage pipeline with deterministic stubs and verifies all artifacts land on disk"
    - "Coverage measurement is enabled with baseline enforcement (no drop below current)"
    - "Coverage reports are generated in a format suitable for CI"
  artifacts:
    - path: "src/__tests__/e2e-canary.test.ts"
      provides: "Full 13-stage canary integration test"
      contains: "describe('Canary: Full 13-Stage Pipeline'"
      min_lines: 200
    - path: "vitest.config.ts"
      provides: "Coverage configuration with baseline thresholds"
      contains: "coverage"
  key_links:
    - from: "src/__tests__/e2e-canary.test.ts"
      to: "src/core/run-manager.ts"
      via: "RunManager.startRun() drives the full pipeline"
      pattern: "RunManager"
    - from: "src/__tests__/e2e-canary.test.ts"
      to: "src/__tests__/test-helpers.ts"
      via: "uses createTestMosaicDir, createMockProvider from Plan 01"
      pattern: "import.*test-helpers"
---

<objective>
Create a canary E2E test for the full 13-stage pipeline and establish coverage baseline.

Purpose: TEST-03 requires an end-to-end canary test using all real modules except LLM (deterministic stub). This is the ultimate safety net — if the canary breaks during rewrite, something fundamental is wrong. Coverage baseline (D-09) ensures the rewrite never drops test coverage below current levels.

Output: New `e2e-canary.test.ts` with full pipeline verification; updated `vitest.config.ts` with coverage settings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-infrastructure-hardening/01-CONTEXT.md

@src/__tests__/e2e-phase3.test.ts

<interfaces>
<!-- Full 13-stage pipeline profile from config/pipeline.yaml -->
Full profile stage order:
  intent_consultant, researcher, product_owner, ux_designer, api_designer,
  ui_designer, tech_lead, qa_lead, coder, tester, security_auditor, reviewer, validator

<!-- RunManager interface -->
From src/core/run-manager.ts:
```typescript
export class RunManager {
  startRun(instruction: string, autoApprove: boolean): Promise<string>;
  waitForRun(runId: string): Promise<PipelineRun>;
  getStatus(runId: string): { state: string; stages: ... } | undefined;
}
```

<!-- Agent outputs from config/agents.yaml (what must exist on disk after full run) -->
Stage artifacts:
- intent_consultant: intent-brief.json
- researcher: research.md, research.manifest.json
- product_owner: prd.md, prd.manifest.json
- ux_designer: ux-flows.md, ux-flows.manifest.json
- api_designer: api-spec.yaml, api-spec.manifest.json
- ui_designer: components/, previews/, screenshots/, gallery.html, ui-plan.json, components.manifest.json
- tech_lead: tech-spec.md, tech-spec.manifest.json
- qa_lead: test-plan.md, test-plan.manifest.json
- coder: code-plan.json, code/, code.manifest.json
- tester: test-report.md, test-report.manifest.json
- security_auditor: security-report.md, security-report.manifest.json
- reviewer: review-report.md, review.manifest.json
- validator: validation-report.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create full 13-stage canary E2E test</name>
  <files>src/__tests__/e2e-canary.test.ts</files>
  <read_first>
    - src/__tests__/e2e-phase3.test.ts (pattern reference — design-only profile E2E)
    - src/__tests__/e2e-phase4.test.ts (pattern reference — full profile agents)
    - src/__tests__/e2e-phase5.test.ts (pattern reference — full profile with tester-coder loop)
    - src/__tests__/test-helpers.ts (mock factories from Plan 01)
    - config/agents.yaml (agent input/output contracts — what each stage produces)
    - config/pipeline.yaml (full profile stage list, gate settings)
    - src/core/run-manager.ts (RunManager API)
    - src/agents/index.ts (all agent exports)
    - src/core/agent-factory.ts (createAgent signature)
  </read_first>
  <action>
Create `src/__tests__/e2e-canary.test.ts` per D-06, D-07, D-08.

**Structure:**
Follow the established e2e-phase3.test.ts pattern but expanded for full 13-stage profile.

**MockLLMProvider for 13 stages:**
Create a `CanaryStubProvider` class implementing `LLMProvider` that returns deterministic JSON responses for each stage. The provider detects which stage is calling based on `_options?.systemPrompt` content (same pattern as e2e-phase3.test.ts).

For the 7 stages already covered in e2e-phase3 (intent_consultant, researcher, product_owner, ux_designer, api_designer, ui_designer, validator): reuse the same response fixtures from e2e-phase3.test.ts.

For the 6 new stages, create minimal valid responses:
- **tech_lead**: Return JSON with `artifact` (markdown tech-spec) and `manifest` (with `architecture`, `modules`, `dependencies` fields).
- **qa_lead**: Return JSON with `artifact` (markdown test-plan) and `manifest` (with `test_suites`, `coverage_targets` fields).
- **coder**: This agent uses multi-pass (planner + builder). Detect via systemPrompt:
  - Planner phase: return a `code-plan.json` with 1 module (e.g., `{ modules: [{ name: "task-api", files: ["code/src/api.ts"], purpose: "API handler" }] }`)
  - Builder phase: return code file content wrapped in ARTIFACT markers
- **tester**: Return JSON with `artifact` (markdown test-report with PASS verdict) and `manifest`.
- **security_auditor**: Return JSON with `artifact` (markdown security-report, no critical findings) and `manifest`.
- **reviewer**: Return JSON with `artifact` (markdown review-report, approved) and `manifest`.

**Mock agent-factory.js:**
Same pattern as e2e-phase3: mock `createAgent` to import all 13 agent classes and instantiate with typed parameters (using `LLMProvider` and `Logger` types, NOT `as any`).

Import all agent classes:
```typescript
import { ResearcherAgent } from '../agents/researcher.js';
import { ProductOwnerAgent } from '../agents/product-owner.js';
// ... all 13 agents
```

**Mock provider-factory.js:**
```typescript
vi.mock('../core/provider-factory.js', () => ({
  createProvider: () => new CanaryStubProvider(),
}));
```

**Test assertions (per D-08):**
1. Pipeline completes (`result.completedAt` is defined)
2. All 13 stage artifacts exist on disk:
   - `intent-brief.json`
   - `research.md`, `research.manifest.json`
   - `prd.md`, `prd.manifest.json`
   - `ux-flows.md`, `ux-flows.manifest.json`
   - `api-spec.yaml`, `api-spec.manifest.json`
   - `components/` directory with at least 1 file
   - `previews/` directory with at least 1 file
   - `gallery.html`, `ui-plan.json`, `components.manifest.json`
   - `tech-spec.md`, `tech-spec.manifest.json`
   - `test-plan.md`, `test-plan.manifest.json`
   - `code-plan.json`, `code.manifest.json`
   - `test-report.md`, `test-report.manifest.json`
   - `security-report.md`, `security-report.manifest.json`
   - `review-report.md`, `review.manifest.json`
   - `validation-report.md`
3. All manifest files parse as valid JSON
4. Manifest files contain expected top-level keys (not empty objects)

Use 120000ms timeout (2 minutes) for the test since 13 stages is heavy.

**Important:** Do NOT use `as any` anywhere. Use typed imports from `test-helpers.ts` for any factory needs. The `createAgent` mock must use `LLMProvider` and `Logger` types.
  </action>
  <verify>
    <automated>cd /Users/lddmay/AiCoding/mosaicat && npx vitest run src/__tests__/e2e-canary.test.ts --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `src/__tests__/e2e-canary.test.ts` exists and contains `describe('Canary: Full 13-Stage Pipeline'`
    - File contains assertions for `tech-spec.md`, `test-plan.md`, `code-plan.json`, `test-report.md`, `security-report.md`, `review-report.md`
    - `grep -n "as any" src/__tests__/e2e-canary.test.ts` returns zero matches
    - `npx vitest run src/__tests__/e2e-canary.test.ts` exits with code 0
    - Test verifies at least 20 artifact files exist on disk
  </acceptance_criteria>
  <done>Full 13-stage canary test passes with deterministic stub responses. All artifacts verified on disk. Zero `as any` casts.</done>
</task>

<task type="auto">
  <name>Task 2: Configure coverage measurement with baseline enforcement</name>
  <files>vitest.config.ts</files>
  <read_first>
    - vitest.config.ts
    - package.json (check if @vitest/coverage-v8 or @vitest/coverage-istanbul is already a dependency)
  </read_first>
  <action>
Per D-09, D-11: Enable coverage measurement and establish baseline enforcement.

**Step 1: Install coverage provider (if not already present)**
Run: `npm install --save-dev @vitest/coverage-v8`

**Step 2: Update vitest.config.ts**
Add coverage configuration:
```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    fileParallelism: false,
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json', 'html'],
      reportsDirectory: './coverage',
      include: ['src/**/*.ts'],
      exclude: [
        'src/**/__tests__/**',
        'src/**/*.test.ts',
        'src/mcp-entry.ts',
      ],
    },
  },
});
```

**Step 3: Measure baseline**
Run `npx vitest run --coverage` and note the line coverage percentage from the output. Then update vitest.config.ts to add threshold enforcement:
```typescript
coverage: {
  // ... reporter config from above ...
  thresholds: {
    lines: <measured_baseline>,   // e.g., lines: 25 (whatever the current number is)
    // Do NOT set branches/functions/statements yet — only lines per D-09
  },
},
```

Round the baseline DOWN to the nearest integer (e.g., if 27.3%, set threshold to 27). This ensures the threshold is achievable and only catches drops.

**Step 4: Add coverage/ to .gitignore**
Check if `coverage/` is already in `.gitignore`. If not, append `coverage/` on a new line.

Per D-10: Do NOT set 80% threshold now — that is deferred to Phase 5.
  </action>
  <verify>
    <automated>cd /Users/lddmay/AiCoding/mosaicat && npx vitest run --coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` contains `coverage:` configuration block
    - `vitest.config.ts` contains `provider: 'v8'`
    - `vitest.config.ts` contains `thresholds:` with a `lines:` value > 0
    - `vitest.config.ts` contains `reporter:` with at least `'text'` and `'json'`
    - `npx vitest run --coverage` exits with code 0 (threshold met)
    - `coverage/` directory exists after run (reports generated)
    - `.gitignore` contains `coverage/` or `coverage`
  </acceptance_criteria>
  <done>Coverage measurement enabled. Baseline threshold enforced. Reports generated in text+json+html formats for CI visibility.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/__tests__/e2e-canary.test.ts` — canary passes
2. `npx vitest run --coverage` — all tests pass with coverage above threshold
3. `grep -n "as any" src/__tests__/e2e-canary.test.ts` — zero matches
4. `ls coverage/` — reports exist
</verification>

<success_criteria>
- Canary test exercises all 13 stages with deterministic stubs (D-06, D-07)
- All stage artifacts verified on disk after pipeline run (D-08)
- Coverage baseline measured and enforced as threshold (D-09)
- Coverage reports generated for CI (D-11)
</success_criteria>

<output>
After completion, create `.planning/phases/01-test-infrastructure-hardening/01-03-SUMMARY.md`
</output>
