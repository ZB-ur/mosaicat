---
phase: 04-coder-decomposition
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/agents/coder/build-verifier.ts
  - src/agents/coder/smoke-runner.ts
  - src/agents/__tests__/build-verifier.test.ts
  - src/agents/__tests__/smoke-runner.test.ts
autonomous: true
requirements: [CODER-03, CODER-04, TEST-04]

must_haves:
  truths:
    - "BuildVerifier runs setup/verify/build shell commands and returns success/error results"
    - "BuildVerifier executes build-fix loops with LLM-assisted error correction"
    - "BuildVerifier runs acceptance tests and fix cycles when test files exist"
    - "BuildVerifier prompts user via InteractionHandler when AUTO_FIX_RETRIES exceeded"
    - "SmokeRunner starts a server process and probes the port for readiness"
    - "SmokeRunner skips smoke tests for non-web project types"
    - "SmokeRunner cleans up spawned processes in finally blocks"
    - "Shell command execution paths (setup/build/verify/smoke-test) have unit test coverage"
  artifacts:
    - path: "src/agents/coder/build-verifier.ts"
      provides: "BuildVerifier class with shell command execution and build-fix loops"
      exports: ["BuildVerifier"]
    - path: "src/agents/coder/smoke-runner.ts"
      provides: "SmokeRunner class with HTTP probe and port waiting"
      exports: ["SmokeRunner"]
    - path: "src/agents/__tests__/build-verifier.test.ts"
      provides: "Unit tests for BuildVerifier shell command paths"
      min_lines: 80
    - path: "src/agents/__tests__/smoke-runner.test.ts"
      provides: "Unit tests for SmokeRunner shell command paths"
      min_lines: 60
  key_links:
    - from: "src/agents/coder/build-verifier.ts"
      to: "node:child_process"
      via: "execSync for setup/verify/build commands"
      pattern: "execSync"
    - from: "src/agents/coder/smoke-runner.ts"
      to: "node:child_process"
      via: "spawn for server process"
      pattern: "spawn"
    - from: "src/agents/coder/smoke-runner.ts"
      to: "node:net"
      via: "net.Socket for port probing"
      pattern: "net\\.Socket|new Socket"
---

<objective>
Extract BuildVerifier and SmokeRunner from coder.ts into standalone sub-modules, with comprehensive shell command execution path tests (TEST-04).

Purpose: These two sub-modules handle the "verification" side of the Coder pipeline (compilation checks, build-fix loops, acceptance tests, HTTP smoke tests). TEST-04 specifically requires testing the shell command execution paths in these modules.

Output: 2 new source files in src/agents/coder/ + 2 test files covering all 4 shell command paths (setup/build/verify/smoke-test)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-coder-decomposition/04-RESEARCH.md

@src/agents/coder.ts
@src/agents/code-plan-schema.ts
@src/core/agent.ts
@src/core/llm-provider.ts
@src/core/artifact-store.ts
@src/core/event-bus.ts
@src/core/interaction-handler.ts
@src/core/retry-log.ts
@src/__tests__/test-helpers.ts

<interfaces>
<!-- Types created by Plan 01 that this plan uses -->

From src/agents/coder/types.ts (created by Plan 01):
```typescript
export interface CoderDeps {
  readonly stage: StageName;
  readonly provider: LLMProvider;
  readonly store: ArtifactStore;
  readonly logger: Logger;
  readonly eventBus: EventBus;
}

export interface BuildVerifierDeps extends CoderDeps {
  readonly interactionHandler?: InteractionHandler;
}

export interface SmokeRunnerDeps {
  readonly stage: StageName;
  readonly store: ArtifactStore;
  readonly logger: Logger;
  readonly eventBus: EventBus;
}
```

From src/agents/coder/utils.ts (created by Plan 01):
```typescript
export function listBuiltFiles(codeDir: string): string[];
export function walkDir(dir: string, base?: string): string[];
```

From src/agents/coder/coder-builder.ts (created by Plan 01):
```typescript
export const BUILDER_PROMPT_PATH: string;  // '.claude/agents/mosaic/code-builder.md'
```

From src/core/interaction-handler.ts:
```typescript
export interface InteractionHandler {
  onClarification(stage: StageName, question: string, context: string, options: { label: string; description: string }[], required: boolean): Promise<string>;
  onApproval(stage: StageName, summary: string, artifacts: string[]): Promise<GateResult>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create BuildVerifier sub-module with shell command path tests</name>
  <files>
    src/agents/coder/build-verifier.ts,
    src/agents/__tests__/build-verifier.test.ts
  </files>
  <read_first>
    src/agents/coder.ts (lines 236-384 for acceptance tests, lines 726-746 for askUserToRetry, lines 748-864 for build fix + analyze, lines 1008-1079 for setup/verify/build/extractErrorFiles),
    src/agents/coder.ts (lines 1-35 for constants: AUTO_FIX_RETRIES, PLACEHOLDER_KEYWORDS, MIN_BUNDLE_SIZE_BYTES, MIN_HTML_LENGTH, BUILD_FIX_TIMEOUT_MS),
    src/core/interaction-handler.ts,
    src/core/retry-log.ts (for logRetry, classifyError imports),
    src/__tests__/test-helpers.ts
  </read_first>
  <behavior>
    - Test: runSetupCommand() calls execSync with plan.commands.setupCommand in codeDir
    - Test: runSetupCommand() logs warning on failure but does not throw
    - Test: runVerifyCommand() returns { success: true } when execSync succeeds
    - Test: runVerifyCommand() returns { success: false, errors: string } when execSync throws
    - Test: runBuildCommand() returns { success: true } when execSync succeeds
    - Test: runBuildCommand() returns { success: false, errors: string } when execSync throws
    - Test: extractErrorFiles() parses TypeScript error format "src/foo.ts(10,5):" correctly
    - Test: analyzeBuildArtifacts() logs placeholder warnings and bundle size analysis
    - Test: askUserToRetry() returns false when no interactionHandler provided
    - Test: runAcceptanceTests() skips when no test-plan.md exists
  </behavior>
  <action>
    1. Create `src/agents/coder/build-verifier.ts`:
       - Class `BuildVerifier` with constructor taking `BuildVerifierDeps` (from types.ts)
       - Import `BuildVerifierDeps` from `./types.js`, `BUILDER_PROMPT_PATH` from `./coder-builder.js`, `listBuiltFiles` from `./utils.js`
       - Move constants: `AUTO_FIX_RETRIES = 3`, `BUILD_FIX_TIMEOUT_MS = 600_000`, `PLACEHOLDER_KEYWORDS`, `MIN_BUNDLE_SIZE_BYTES = 10_000`, `MIN_HTML_LENGTH = 500`
       - Method `runSetupCommand(plan: CodePlan): void` — extract from lines 1010-1024. Uses `execSync(plan.commands.setupCommand, { cwd: codeDir, stdio: 'pipe' })` where `codeDir = this.deps.store.getDir() + '/code'`. Wraps in try/catch, logs warning on failure.
       - Method `runVerifyCommand(plan: CodePlan): { success: boolean; errors: string }` — extract from lines 1026-1041. Returns `{ success: true, errors: '' }` on success, `{ success: false, errors: stderr+stdout }` on throw.
       - Method `runBuildCommand(plan: CodePlan): { success: boolean; errors: string }` — extract from lines 1043-1058. Same pattern as verify.
       - Method `async runBuildFix(context: AgentContext, plan: CodePlan, errors: string, budgetUsd: number): Promise<void>` — extract from lines 750-786. Uses BUILDER_PROMPT_PATH imported from coder-builder.
       - Method `analyzeBuildArtifacts(plan: CodePlan): void` — extract from lines 794-852. Zero LLM cost analysis.
       - Method `logAnalysisResult(label: string, passed: boolean, details: string): void` — extract from lines 854-864.
       - Method `extractErrorFiles(errors: string): string[]` — extract from lines 1065-1079.
       - Method `async runAcceptanceTests(context: AgentContext, plan: CodePlan, budgetUsd: number): Promise<void>` — extract from lines 242-320. Checks store.exists('test-plan.md') first.
       - Method `async executeAcceptanceTests(plan: CodePlan): { passed: boolean; output: string }` — extract from lines 322-350.
       - Method `async fixAcceptanceFailures(context: AgentContext, plan: CodePlan, testOutput: string, budgetUsd: number): Promise<void>` — extract from lines 352-384.
       - Method `async askUserToRetry(moduleName: string, attempts: number, errors: string): Promise<boolean>` — extract from lines 728-746. Uses `this.deps.interactionHandler?.onClarification()`. Returns false if no handler.

    2. Write tests in `src/agents/__tests__/build-verifier.test.ts`:
       - Use `vi.mock('node:child_process')` to mock `execSync`
       - Create BuildVerifierDeps using test helpers (createTestArtifactStore, createMockProvider, createMockLogger, new EventBus)
       - Test runSetupCommand: verify execSync called with correct command and cwd
       - Test runSetupCommand: verify warning logged on failure (mock execSync to throw)
       - Test runVerifyCommand: success case returns { success: true }
       - Test runVerifyCommand: failure case returns { success: false, errors: 'error text' }
       - Test runBuildCommand: same success/failure pattern
       - Test extractErrorFiles: input `"src/foo.ts(10,5): error TS2304\nsrc/bar.ts(3,1): error"` returns `['src/foo.ts', 'src/bar.ts']`
       - Test analyzeBuildArtifacts: logs results (mock fs operations as needed)
       - Test askUserToRetry: returns false when deps.interactionHandler is undefined
       - Test runAcceptanceTests: skips when test-plan.md does not exist in store
       - These tests satisfy TEST-04 for setup/build/verify paths
  </action>
  <verify>
    <automated>cd /Users/lddmay/AiCoding/mosaicat && npx vitest run src/agents/__tests__/build-verifier.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export class BuildVerifier' src/agents/coder/build-verifier.ts` returns 1
    - `grep -c 'execSync' src/agents/coder/build-verifier.ts` returns at least 3 (setup, verify, build)
    - `grep -c 'runSetupCommand' src/agents/coder/build-verifier.ts` returns at least 1
    - `grep -c 'runVerifyCommand' src/agents/coder/build-verifier.ts` returns at least 1
    - `grep -c 'runBuildCommand' src/agents/coder/build-verifier.ts` returns at least 1
    - `grep -c 'askUserToRetry' src/agents/coder/build-verifier.ts` returns at least 1
    - `grep -c 'runAcceptanceTests' src/agents/coder/build-verifier.ts` returns at least 1
    - `grep 'vi.mock' src/agents/__tests__/build-verifier.test.ts` matches (child_process is mocked)
    - All tests in build-verifier.test.ts pass
  </acceptance_criteria>
  <done>BuildVerifier is a standalone class with all compilation/build/acceptance test logic extracted from coder.ts. Shell command paths (setup/build/verify) have mocked unit tests satisfying TEST-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SmokeRunner sub-module with shell command path tests</name>
  <files>
    src/agents/coder/smoke-runner.ts,
    src/agents/__tests__/smoke-runner.test.ts
  </files>
  <read_first>
    src/agents/coder.ts (lines 866-1006 for runSmokeTest + waitForPort, lines 28-29 for SMOKE_TEST_TIMEOUT_MS),
    src/agents/coder/types.ts (SmokeRunnerDeps),
    src/agents/code-plan-schema.ts (SmokeTest type),
    src/__tests__/test-helpers.ts
  </read_first>
  <behavior>
    - Test: runSmokeTest() skips when plan.smokeTest is undefined
    - Test: runSmokeTest() skips for non-web types (cli, library)
    - Test: runSmokeTest() spawns process with startCommand and calls waitForPort
    - Test: runSmokeTest() kills spawned process in finally block
    - Test: waitForPort() resolves true when port connects
    - Test: waitForPort() resolves false on timeout
    - Test: waitForPort() resolves true when readyPattern matched in stdout
  </behavior>
  <action>
    1. Create `src/agents/coder/smoke-runner.ts`:
       - Class `SmokeRunner` with constructor taking `SmokeRunnerDeps` (no LLMProvider needed)
       - Move constant: `SMOKE_TEST_TIMEOUT_MS = 15_000`
       - Method `async runSmokeTest(plan: CodePlan): Promise<void>` — extract from lines 872-949. Replace this.ctx.store/logger/eventBus with this.deps.*. Key flow: skip if no smokeTest config, skip if type is 'cli' or 'library', spawn(startCommand, { cwd, shell: true }), waitForPort, make HTTP request to localhost:port, kill process in finally.
       - Method `waitForPort(port: number, timeoutMs: number, readyPattern?: string, proc?: ChildProcess): Promise<boolean>` — extract from lines 955-1006. Uses `net.Socket` to probe port, checks proc.stdout for readyPattern match. Returns boolean.
       - Import `spawn, type ChildProcess` from `node:child_process`, `net` from `node:net`.

    2. Write tests in `src/agents/__tests__/smoke-runner.test.ts`:
       - Use `vi.mock('node:child_process')` to mock `spawn`
       - Use `vi.mock('node:net')` to mock `net.Socket` (or use the real net module with a test TCP server)
       - Create SmokeRunnerDeps using test helpers
       - Test runSmokeTest: skips when plan.smokeTest is undefined (verify logger not called with 'smoke:start')
       - Test runSmokeTest: skips for type 'library' and 'cli'
       - Test runSmokeTest: spawns process for type 'web' with correct startCommand
       - Test runSmokeTest: kills process in finally (verify proc.kill or process.kill called)
       - Test waitForPort: resolves true when socket connects (mock Socket.connect to emit 'connect')
       - Test waitForPort: resolves false on timeout (mock Socket.connect to never emit)
       - These tests satisfy TEST-04 for smoke-test path
  </action>
  <verify>
    <automated>cd /Users/lddmay/AiCoding/mosaicat && npx vitest run src/agents/__tests__/smoke-runner.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export class SmokeRunner' src/agents/coder/smoke-runner.ts` returns 1
    - `grep -c 'spawn' src/agents/coder/smoke-runner.ts` returns at least 2 (import + usage)
    - `grep -c 'waitForPort' src/agents/coder/smoke-runner.ts` returns at least 2 (declaration + usage)
    - `grep -c 'net.Socket\|new Socket' src/agents/coder/smoke-runner.ts` returns at least 1
    - `grep 'vi.mock' src/agents/__tests__/smoke-runner.test.ts` matches (child_process is mocked)
    - All tests in smoke-runner.test.ts pass
  </acceptance_criteria>
  <done>SmokeRunner is a standalone class with HTTP probe and port-waiting logic. Shell command path (smoke-test) has mocked unit tests satisfying TEST-04. All 4 shell paths (setup/build/verify/smoke-test) now have test coverage across both test files.</done>
</task>

</tasks>

<verification>
```bash
# All new files exist
ls src/agents/coder/build-verifier.ts src/agents/coder/smoke-runner.ts

# All tests pass
npx vitest run src/agents/__tests__/build-verifier.test.ts src/agents/__tests__/smoke-runner.test.ts --reporter=verbose

# TypeScript compiles
npx tsc --noEmit
```
</verification>

<success_criteria>
- BuildVerifier and SmokeRunner are standalone classes with no BaseAgent dependency
- BuildVerifier uses BuildVerifierDeps (extends CoderDeps with optional InteractionHandler)
- SmokeRunner uses SmokeRunnerDeps (no LLMProvider needed)
- All 4 shell command paths (setup/build/verify/smoke-test) have mocked unit tests (TEST-04)
- All tests pass, TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-coder-decomposition/04-02-SUMMARY.md`
</output>
